{
  "version": "1.3.1",
  "notes": "Bundled first push since 1.3.0: schedule_sync push-only fix (no more silent overwrite of local edits), connection-flapping fix (removed dueling iOS polling timers), 30-min cloud-sync interval, and automatic/manual NTP clock resync."
}
